## Data Import Supabase Save

This change replaces the browser-only placeholder with a backend-driven OHLC import workflow. It adds multipart upload support, backend preview validation, database status/init calls, database upsert, dynamic readiness messaging, progress states, import metrics, and verified database audit counts.
