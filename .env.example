# DSE Pulse backend base URL. Do not include a trailing slash.
# Local development example:
VITE_DSE_API_BASE_URL=http://localhost:8000

# Production example:
# VITE_DSE_API_BASE_URL=https://dse-pulse-backend-<project>.run.app

# No demo, mock, or synthetic fallback is enabled when this value is missing.
